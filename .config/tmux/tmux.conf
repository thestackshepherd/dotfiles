# ---- General sanity ----
set -g default-terminal "tmux-256color"
set -ga terminal-overrides ",*256col*:Tc"
set -g mouse on
set -g history-limit 10000
set -g escape-time 10
set -g focus-events on

# ---- Prefix (optional: remap to Ctrl-a like screen, comment out to keep Ctrl-b) ----
# set -g prefix C-a
# unbind C-b
# bind C-a send-prefix

# ---- Pane splitting (more intuitive than % and ") ----
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"
unbind '"'
unbind %

# ---- Pane navigation (vim-style) ----
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# ---- Reload config ----
bind r source-file ~/.config/tmux/tmux.conf \; display "Config reloaded!"

# ---- Status bar ----
set -g status-position bottom
set -g status-style bg=default
set -g status-left-length 20
set -g status-right "%Y-%m-%d %H:%M"

# ---- Plugins ----
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'sainnhe/tmux-fzf'
set -g @plugin 'tmux-plugins/tmux-prefix-highlight'

# tmux-continuum: auto-save every 15 min, auto-restore on tmux start
set -g @continuum-restore 'on'
set -g @continuum-save-interval '15'

# tmux-resurrect: also restore pane contents/vim sessions
set -g @resurrect-capture-pane-contents 'on'

# ---- Initialize TPM (keep this line at the very bottom) ----
run '~/.config/tmux/plugins/tpm/tpm'
